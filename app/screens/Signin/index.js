import React, { PureComponent } from 'react';
import { Text, View } from 'react-native';

class Signin extends PureComponent {
  static componentName = 'rnn.screens.signin';

  render() {
    return (
      <View>
        <Text>Signin Screen</Text>
      </View>
    );
  }
}

export default Signin;
